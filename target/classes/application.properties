spring.datasource.url=jdbc:mysql://localhost:3306/agrovalue_connect
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

app.jwt.secret=${JWT_SECRET:changemechangemechangemechangeme}

stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_replace}
stripe.public-key=${STRIPE_PUBLIC_KEY:pk_test_replace}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:replace}

spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=${MAILTRAP_USERNAME:username}
spring.mail.password=${MAILTRAP_PASSWORD:password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

server.port=8080
